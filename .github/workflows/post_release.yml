name: 🗣️ Post Release

permissions:
  contents: read

on:
  workflow_dispatch:
  release:
    types: [published]

jobs:

  publish-cocoapods:
    name: 🫛 Publish Cocoapods
    uses: ./.github/workflows/publish_podspec.yml
